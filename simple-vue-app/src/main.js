import { createApp } from 'vue' // Import Vue's createApp function
import App from './App.vue' // Import the main App component

createApp(App).mount('#app') // Create the app and attach it to the #app element